{
  "name": "bus-fyi-as",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate:stops && vite build",
    "download:gtfs": "node src/download-gtfs.mjs",
    "generate:stops": "node src/generate-stops.mjs",
    "preview": "vite preview",
    "test": "node tests",
    "start": "npx serve ."
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
